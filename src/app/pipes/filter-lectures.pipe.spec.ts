import { FilterLecturesPipe } from './filter-lectures.pipe';

describe('FilterLecturesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLecturesPipe();
    expect(pipe).toBeTruthy();
  });
});
